# HYOJO 要件定義書 v2.1

v2.0 を正本とし、本書は Mobile-first / Huddle memory / adaptive UI の追補である。

## 17. Mobile-first operating model

- 日常利用（Home / Speak / Detail / Huddle の受信・参加・閲覧・承認）は、スマートフォンだけで完結する。
- PC は日常利用の前提にしない。管理者向けの S5 Admin と、大型画面共有の補助表示のみ Web を許容する。
- 日常UIに PC 固有のサイドバー、一覧、複数ペインは持ち込まない。
- Home は「一度開くとAIが語る」単一カラムの体験を維持する。

## 18. Huddle memory

### 18.1 記録と利用

- 記録対象は音声、映像、画面共有、リアルタイム文字起こしである。
- Huddle 終了後、AI は文字起こしを話者・言語・時刻付きで保存し、要約、決定、TODO、関連する既存の Institutional Memory を生成する。
- 次回の Home / Detail / Speak は、この構造化された記憶をコンテキストとして使用する。
- 映像は証跡、文字起こしと構造化要約は組織記憶として扱う。AI 推論は原則として後者を使う。

### 18.2 透明性・保持

- 記録中は常時、参加者全員に「録画・文字起こし中」を明示する。
- Space ごとに、録画可否、音声のみ、保持期限、AI 要約・検索への利用可否を設定できる。
- デフォルトは保持期限付き。延長保存は管理ポリシーまたは明示操作で行う。
- 「記録しない」Huddle を提供する。その場合、文字起こし・要約・AIコンテキストへの追加を行わない。

## 19. Adaptive UI（AI が必要な形で語る）

- 固定の日常画面は S1〜S5 を超えない。
- AI は、情報に応じて Detail 内に一時的な `Adaptive Surface` を提示できる。
- 許可する Surface は、承認カード、比較表、KPI、選択肢、タイムライン、関係図、短いフォーム、30秒サマリーである。
- Surface はユーザーが探すダッシュボードではない。AI の語りの途中で、判断を容易にするためだけに現れる。
- UI は、ホスト側で登録・審査した部品とデータスキーマから組み立てる。任意コード／任意ネットワーク通信／権限外データへのアクセスは許可しない。
- 書込み・外部実行・保持操作は、既存の ACL、承認、監査ログ、可逆性を必須とする。
- Vendo は Adaptive Surface の PoC 候補とする。MVP では永続的なユーザー製ミニアプリではなく、許可済み部品による一時 Surface に限定する。

## 20. Phase 1 追記

- モバイル完結の S1〜S4
- Huddle の音声記録、リアルタイム文字起こし、記録状態の常時表示
- 要約・決定・TODO の Huddle memory 化
- Adaptive Surface の最小セット：承認、比較、KPI、選択肢、30秒サマリー
- Space ごとの記録ポリシーと保持期限

## 21. 残論点

1. **記録の既定値**：音声＋文字起こしを標準 ON にするか、Space 作成者の明示 ON にするか。
2. **映像保持**：映像を何日保持するか。文字起こしのみを長期記憶にするか。
3. **記録しない会議**：記録なしの場合も、参加者が手動で決定だけ残せるか。
4. **スマホでの画面共有**：小画面で共有画面を見る時の優先順位（全画面、字幕、参加者）をどう切り替えるか。
5. **Adaptive Surface の所有期間**：AI が出したUIを即時消すか、Detailに期限付きで残すか。
6. **生成の境界**：Phase 1 は固定コンポーネントの構成に限定し、任意コード生成は Phase 2 以降にするか。
7. **コスト上限**：テナントごとの録画時間、文字起こし、翻訳、生成UI推論の予算と停止・劣化ポリシー。
8. **データ所在地と削除**：映像・音声・文字起こし・要約それぞれの保存リージョン、削除猶予、監査要件。

